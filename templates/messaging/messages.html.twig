{% extends 'layout.html.twig' %}


{% block content %}
 <div class="row">
    {% include 'aside.html.twig' %}
    <div class="col-md-8">

       {% for message in messages %}
           {{ message.author }}
           {{ message.recipient }}
           {{ message.datemessage|date('d/m/Y') }}
           {{ message.message }}
       {% else %}
           aucun message
       {% endfor %}
    
    </div>
</div>   
{% endblock %}
